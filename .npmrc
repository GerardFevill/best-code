# Configuration NPM pour @relia-scope/hello-utils

# Registre par défaut
registry=https://registry.npmjs.org/

# Configuration pour les packages scoped
@relia-scope:registry=https://registry.npmjs.org/

# Accès public pour les packages scoped
access=public

# Optionnel: Configuration du tag par défaut
tag=latest

# Optionnel: Sauvegarder les dépendances exactes
save-exact=true
